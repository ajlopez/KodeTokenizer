var Types = { Digits: 1, Word: 2 };function getTokens(text) {    var ch = text[0];        if (isLetter(ch))        return [{ value: text, type: Types.Word }];            return [{ value: text, type: Types.Digits }];}function isLetter(ch) {    if (ch >= 'a' && ch <= 'z')        return true;    if (ch >= 'A' && ch <= 'Z')        return true;    return false;}module.exports = {    getTokens: getTokens,    Types: Types};